<div class="drawer" data-controller="drawer" data-drawer-previous-url-value="<%= @previous_url %>">
  <div class="overlay" data-action="click->drawer#close"></div>
  <div class="body">
    <%= turbo_frame_tag 'drawer-frame' do %>
      <% if content.present? %>
        <%= content %>
      <% else %>
        <p>Loading ...</p>
      <% end %>
    <% end %>
  </div>
</div>
